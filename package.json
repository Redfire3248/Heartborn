{
  "name": "heartborn",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "slice": "python tools/slice_sheets.py",
    "deploy": "vite build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^12.19.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
